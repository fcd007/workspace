package br.univel.patterns.structural.bridge;

public class Bridge {

	/**
	 * Um exemplo de bridge é a utilizacao de interfaces
	 * do jdbc. Durante a implementacao nao se sabe qual
	 * implementacao do jdbc vai ser utilizada, entao usa-se
	 * interfaces como ponte (bridge), ou seja, o codigo e
	 * feito referenciando interfaces.
	 */
	
}
